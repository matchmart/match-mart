import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import toast from "react-hot-toast";
import axios from "axios";
import { FiArrowRight, FiGrid } from "react-icons/fi";
import * as categoryService from "@/services/categoryService";
import type { Category } from "@/types/category";
const getErrorMessage=(e:unknown,f:string)=>axios.isAxiosError<{message?:string}>(e)?e.response?.data?.message||e.message||f:e instanceof Error?e.message:f;
const CategoriesPage=()=>{const [categories,setCategories]=useState<Category[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{categoryService.getActiveCategories().then(r=>setCategories(r.categories)).catch(e=>toast.error(getErrorMessage(e,'Failed to load categories'))).finally(()=>setLoading(false))},[]);return <div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="text-center"><h1 className="text-3xl font-bold text-secondary">Shop by Category</h1><p className="mt-2 text-sm text-secondary/60">Browse collections organized for you</p></div>{loading?<div className="mt-10 grid grid-cols-2 gap-6 sm:grid-cols-3 lg:grid-cols-4">{Array.from({length:8}).map((_,i)=><div key={i} className="animate-pulse rounded-2xl border border-gray-100 p-3"><div className="aspect-square rounded-xl bg-gray-100"/></div>)}</div>:categories.length===0?<div className="mt-10 flex h-60 flex-col items-center justify-center rounded-2xl border border-dashed border-gray-200"><FiGrid className="text-3xl text-secondary/20"/><p className="mt-3 text-sm text-secondary/50">No categories available yet.</p></div>:<div className="mt-10 grid grid-cols-2 gap-6 sm:grid-cols-3 lg:grid-cols-4">{categories.map(c=><Link key={c._id} to={`/products?category=${c._id}`} className="group overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-sm hover:shadow-md"><div className="flex aspect-square items-center justify-center overflow-hidden bg-gray-50">{c.imageUrl?<img src={c.imageUrl} alt={c.name} className="h-full w-full object-cover transition-transform group-hover:scale-105"/>:<FiGrid className="text-3xl text-secondary/20"/>}</div><div className="flex items-center justify-between p-4"><div><h3 className="font-medium text-secondary">{c.name}</h3>{c.description&&<p className="mt-0.5 line-clamp-1 text-xs text-secondary/50">{c.description}</p>}</div><FiArrowRight className="text-secondary/30 group-hover:translate-x-1 group-hover:text-primary"/></div></Link>)}</div>}</div>};
export default CategoriesPage;
